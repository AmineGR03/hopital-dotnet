@page
@model LockoutModel
@{
    ViewData["Title"] = "Compte verrouillé";
    Layout = "/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="row justify-content-center align-items-center" style="min-height: calc(100vh - 300px);">
        <div class="col-md-5 col-lg-4">
            <div class="card shadow-lg border-0">
                <div class="card-body p-5 text-center">
                    <div class="mb-4">
                        <i class="fas fa-lock fa-4x text-danger"></i>
                    </div>
                    <h2 class="fw-bold mb-3">Compte verrouillé</h2>
                    <p class="text-muted mb-4">
                        Ce compte a été verrouillé, veuillez réessayer plus tard.
                    </p>
                    <a asp-page="./Login" class="btn btn-primary">
                        <i class="fas fa-arrow-left me-2"></i>Retour à la connexion
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

